# VirusTotal Checker 🔍

Python-скрипт для проверки доменов и хешей файлов через [VirusTotal API v3](https://docs.virustotal.com/reference/overview).

---

## Возможности

- Проверка **домена** — статистика детектов, категории, репутация
- Проверка **хеша файла** (MD5 / SHA1 / SHA256) — имя, тип, размер, список сработавших антивирусов
- Автоматическое определение типа входных данных
- Сохранение сырого JSON-ответа API в файл
- Загрузка API-ключа из файла `.env`

---

## Требования

- Python 3.8+
- Библиотеки: `requests`, `python-dotenv`

---

## Установка

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/<ваш-username>/<название-репо>.git
cd <название-репо>

# 2. Установите зависимости
pip install requests python-dotenv
```

---

## Настройка API-ключа

1. Зарегистрируйтесь на [virustotal.com](https://www.virustotal.com/)
2. Перейдите в профиль → скопируйте **API Key**
3. Создайте файл `.env` в папке проекта:

```
VT_API_KEY=ваш_ключ_здесь
```

> ⚠️ Файл `.env` добавлен в `.gitignore` — ключ не попадёт в репозиторий.  
> В репозитории есть `.env.example` — шаблон для ознакомления.

---

## Запуск

```bash
python virustotal_checker.py
```

Скрипт запросит ввод — введите домен или хеш файла:

```
  >> Введите значение для проверки: google.com
```

---

## Примеры

**Проверка домена:**
```
  >> Введите значение для проверки: google.com

  Определён тип: DOMAIN
  Отправляем запрос к VirusTotal API...

  =======================================================
    ОТЧЁТ ПО ДОМЕНУ: google.com
  =======================================================
    Вредоносных детектов : 0
    Подозрительных       : 0
    Безопасных           : 71
    Без детекта          : 9
    Категории            : search engines, search engine
    Репутация            : 522
  =======================================================

  JSON-ответ сохранён в файл: vt_domain_google.com.json
```

**Проверка хеша файла:**
```
  >> Введите значение для проверки: 44d88612fea8a8f36de82e1278abb02f

  Определён тип: MD5
  Отправляем запрос к VirusTotal API...

  =======================================================
    ОТЧЁТ ПО ФАЙЛУ (хеш): 44d88612fea8a8f36de82e1278abb02f
  =======================================================
    Имя файла            : eicar.com
    Тип файла            : DOS COM
    Размер               : 68 байт
    Вредоносных детектов : 61
    Подозрительных       : 0
    Безопасных           : 0
    Без детекта          : 12

    Обнаружено следующими антивирусами (61):
      • Kaspersky: EICAR-Test-File
      • BitDefender: EICAR-Test-File (not a virus)
      • ...
  =======================================================

  JSON-ответ сохранён в файл: vt_hash_44d88612fe.json
```

---

## Структура проекта

```
virustotal-check/
├── virustotal_checker.py   # основной скрипт
├── .env                    # API-ключ (не публикуется)
├── .env.example            # шаблон .env
├── .gitignore
└── README.md
```

---

## Ограничения бесплатного плана

| Параметр | Значение |
|---|---|
| Запросов в минуту | 4 |
| Запросов в сутки | 500 |
| Запросов в месяц | 15 500 |

---

## Используемые технологии

- [VirusTotal API v3](https://docs.virustotal.com/reference/overview)
- [requests](https://pypi.org/project/requests/)
- [python-dotenv](https://pypi.org/project/python-dotenv/)